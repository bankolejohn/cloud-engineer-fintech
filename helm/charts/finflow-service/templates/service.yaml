apiVersion: v1
kind: Service
metadata:
  name: {{ include "finflow-service.fullname" . }}
  labels:
    {{- include "finflow-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finflow-service.selectorLabels" . | nindent 4 }}

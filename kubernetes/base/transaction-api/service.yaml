apiVersion: v1
kind: Service
metadata:
  name: transaction-api
  namespace: finflow
  labels:
    app: transaction-api
    app.kubernetes.io/name: transaction-api
    app.kubernetes.io/part-of: finflow
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: transaction-api
